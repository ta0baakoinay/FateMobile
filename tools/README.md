# tools

Asset pipeline scripts: GRF extraction → texture atlas packing → map data →
Android-friendly formats (docs/FATE_MMO_MOBILE_ARCHITECTURE.md §7).

**Status:** empty. No copyrighted Ragnarok/Fate MMO assets are bundled in
this repository — these scripts are written against the server operator's
own legally-held GRF/asset files, run locally, output landing in assets/
(gitignored).
