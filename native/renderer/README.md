# native/renderer

OpenGL ES sprite batching, texture atlases, frustum culling, camera
(docs/FATE_MMO_MOBILE_ARCHITECTURE.md §5).

**Status:** empty beyond the Phase 0/1 JNI stub in native/src/native_lib.cpp
(proves the NDK toolchain links; not a renderer). Real rendering starts
Phase 3 (minimal map load) per docs/FATE_MMO_MOBILE_ROADMAP.md.
