# native/assets

Runtime loading of the converted, Android-optimized asset formats produced by
tools/ (see docs/FATE_MMO_MOBILE_ARCHITECTURE.md §7). Does not read raw GRF —
that conversion happens offline in tools/, once, per operator-supplied asset
set.

**Status:** empty. Populated starting Phase 3.
